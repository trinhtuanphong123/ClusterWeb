### 7.4. Thêm docs/test-matrix/frontend.md

```markdown
# Frontend validation

## Local build

Command:
```bash
cd apps/web
npm run lint
npm run build
UI check

Expected:

page renders without runtime error
loading state exists
error state exists if API call is used
no secret is exposed to client